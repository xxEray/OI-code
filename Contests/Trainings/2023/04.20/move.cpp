#include <cstdio>
#include <algorithm>

const int N = 1e5 + 5;

int n;
int a[N], b[N], c[N], d[N];

struct SegmentTree {

} seg;

void solve() {

}

int main() {
	scanf("%d", &n);
	for(int i = 1; i <= n; i++) scanf("%d%d%d%d", &a[i], &b[i], &c[i], &d[i]);

	return 0;
}