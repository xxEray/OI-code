#include <cstdio>

typedef long long LL;

const int N = 3000 + 5;

char s[N], t[N];
int n;

int main() {
	freopen("block.in", "r", stdin);
	freopen("block.out", "w", stdout);
	scanf("%d%s%s", &n, s + 1, t + 1);
	for(int i = 1; i <= n; i++) 
	return 0;
}