#include <cstdio>
#include <algorithm>

typedef long long LL;

const int N = 1e5 + 5;

LL a[N];
int n;
LL sum;

int main() {
	scanf("%d", &n);
	for(int i = 1; i <= n; i++) scanf("%lld", &a[i]);
	
	return 0;
}