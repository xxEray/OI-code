#include <bits/stdc++.h>

const int N = 5e5 + 5;

int n;
int a[N];

struct SegmentTree {

} seg;

int main() {
	int T; scanf("%d", &T);
	while(T--) {
		scanf("%d", &n);
		for(int i = 1; i <= n; i++) scanf("%d", &a[i]);
		
	}
	return 0;
}